<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when pressed -->
    <item android:state_pressed="true" android:color="@color/lavender" />

    <!-- Default state -->
    <item android:color="@color/grey" />
</selector>
